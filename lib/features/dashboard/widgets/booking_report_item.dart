import 'package:flutter/material.dart';

class BookingReportItem extends StatelessWidget {
  const BookingReportItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
