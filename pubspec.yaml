name: arcturus_mobile_app
description: "Arcturus APP"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  provider: ^6.1.2
  http: ^1.2.1
  flutter_secure_storage: ^9.0.0
  iconsax: ^0.0.8
  url_launcher: ^6.2.6
  shimmer: ^3.0.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  html: ^0.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/vectors/
